
// An interface is a grouping of related methods and constants
// With Java 8 an interface can define default methods
// With Java 8 an interface can define static methods 
interface Controls {
	
	void changeChannel(int channelNumber);
	
	void increaseVolume();
	
	void decreaseVolume();
}
